package com.paymentprocessor.analytics.domain.enums;

/** Recurrence for scheduled reports. Window = the period the report covers. */
public enum ScheduleCadence {
    DAILY,
    WEEKLY,
    MONTHLY
}
