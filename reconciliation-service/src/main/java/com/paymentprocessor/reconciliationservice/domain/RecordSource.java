package com.paymentprocessor.reconciliationservice.domain;

/** Whether a reconciliation record originates from an internal system or an external statement. */
public enum RecordSource {
    INTERNAL,
    EXTERNAL
}
